# Copy this file to .env and adjust as needed.

# Comma-separated list of allowed CORS origins for the frontend
CORS_ORIGINS=http://localhost:5173

# Host/port for uvicorn (used only if you run via a custom launcher)
API_HOST=0.0.0.0
API_PORT=8000
